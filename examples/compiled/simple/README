First set up the links defined with ../pleaseExecuteMe.
This example can be run as an ACLIC on the fly compiled macro

	root -q -b myAnalysis.cpp++

It can be compiled as well

	make 
	./myAnalysis


